import 'package:flutter/material.dart';

class SecondPilot extends StatelessWidget {
  const SecondPilot({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}